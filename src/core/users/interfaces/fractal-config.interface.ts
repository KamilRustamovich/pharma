export interface FractalConfig {
    frontendServer: string
    authServer: string
    resourceServer: string
    client_id: string
    client_secret: string
    scope: string
    redirect_uri: string
    level: string
}
